Primeiro Passo no Git

Primeiro, criamos no github um repositorio com nome GitParaEnsinoMedioNovotec

-Seguinte, fizemos um git clone do repositorio para o nosso pc

-Configuramos o nome do usuario e o email do usuario

-Criamos um Arquivo chamado manual.txt

-Fizemos um commit e finalmente um push na master

Mostramos os códigos seguidos:

git clone (url do repositorio)
git config --global user.name "Ricael Costa Santos" (uma vez só no git)
git config --global user.email "rickaelsantos568@gmail.com" (uma ves só no git)
git status
git add .
git commit -m "Digitamos um texto que possa descrever o que a gente fez
git status
git push (precisaremos depois deste passo passar pelo o nosso usuario e senha do github)

rwqjtpoqjtpojpTOPKJQWTPOJP